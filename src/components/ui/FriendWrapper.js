import styled from 'styled-components';

const FriendWrapper = styled.div`
  label {
    text-align: center;
    width: 100%;
  }

  div {
    display: flex;
    margin-top: 0.6rem;

    input {
      margin-top: 0;
    }
  }
`;

export default FriendWrapper;
